class ChatModel {
  var name;
  var icon;
  var time;
  var status;
  var currentMessage;
  ChatModel({
    required this.name,
    this.icon,
    this.time,
    this.status,
    this.currentMessage,
  });
}
